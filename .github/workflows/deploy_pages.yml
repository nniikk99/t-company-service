name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.5'
        channel: 'stable'
    
    - name: Install dependencies
      run: flutter pub get
    
    - name: Copy supabase config
      run: |
        cp lib/config/supabase_config.example.dart lib/config/supabase_config.dart
        sed -i 's/YOUR_SUPABASE_URL/${{ secrets.SUPABASE_URL }}/g' lib/config/supabase_config.dart
        sed -i 's/YOUR_SUPABASE_ANON_KEY/${{ secrets.SUPABASE_ANON_KEY }}/g' lib/config/supabase_config.dart
    
    - name: Build web
      run: flutter build web --release --base-href /t-company-service/
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: build/web

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
